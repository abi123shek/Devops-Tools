# How to instal prometheus file while permission denied
``` bash  sudo chmod +x install_prometheus.sh```
```bash  sudo ./install_prometheus.sh```
```bash  http://localhost:9090```
by default prometheus port is 9090
